# Runtime dependencies for the AI-Powered Data Chatbot.
# Mirrors pixi.toml for people who prefer a plain venv:
#   python -m venv .venv && pip install -r requirements.txt

PyQt6>=6.9.1,<7
openai>=1.88.0,<2
python-dotenv>=1.1.0,<2
